(() => {

    enum AudioLevel {
        min = 1,
        medium = 5,
        max = 10
    }

    let cuerrentAudio = AudioLevel.max;

    console.log(cuerrentAudio);
    console.log(AudioLevel);
    

})()